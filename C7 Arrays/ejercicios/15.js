array = [123,23,23,1,2,3,5,75,43,6,78,4333,67,4,7,8,54,34]

function encontrarIndiceMayor(array) {

valormax = Math.max (...array); //spread operator. convierte los elementos del arreglo en argumentos individuales para pasar por el math.max
return indicevalormax  = array.indexOf (valormax);
  
  // Encuentra el índice del número más grande en el array de números.
  // Devuelve el valor de este índice.
  // Tu código:
}


console.log (encontrarIndiceMayor(array));
console.log (encontrarIndiceMayor)